#ifndef

struct Vector
{
  City * cityArray; //Don't need cityArray[] because you will 
                    // determine the size of the array later?
  int current_size; 
  int current_count;
};

#endif
